# 停止线对齐原理图解

## 修复前 vs 修复后

### 修复前的问题

```
                停止线
                  |
                  |
    [车辆] ------>| (检测范围: 2px)
           ↑      |
           |      |
        停车位置   |
     (可能在停止线前5px)
```

**问题**：
- 检测范围太小（2px），可能错过检测
- 目标位置有 5px 偏移
- 停车时不调整位置，直接暂停动画

### 修复后的解决方案

```
                停止线
                  |
    检测范围      |
    <-10px->      |
         [车辆]-->|← alignToStopLine() 精确调整
                  |
                  ↑
              车头对齐点
```

**改进**：
- 检测范围扩大到 10px，更可靠
- 目标位置为 0px，精确对齐
- 停车时自动调整到精确位置

## 对齐计算逻辑

### 东向车辆 (→)

```
停止线位置: stopLine.x = 400
车辆宽度: width = 35

计算：
  车辆左上角X = stopLine.x - width
  车辆左上角X = 400 - 35 = 365

结果：
  车头（右侧）= 365 + 35 = 400 ✓ 对齐！

     车辆
  ┌──────┐
  │      │
  │ [车] │→ 车头
  │      │
  └──────┘
  365    400 (停止线)
```

### 西向车辆 (←)

```
停止线位置: stopLine.x = 400
车辆宽度: width = 35

计算：
  车辆左上角X = stopLine.x
  车辆左上角X = 400

结果：
  车头（左侧）= 400 ✓ 对齐！

         车辆
      ┌──────┐
      │      │
车头 ←│ [车] │
      │      │
      └──────┘
  (停止线) 400    435
```

### 南向车辆 (↓)

```
停止线位置: stopLine.y = 300
车辆高度: height = 20

计算：
  车辆左上角Y = stopLine.y - height
  车辆左上角Y = 300 - 20 = 280

结果：
  车头（下方）= 280 + 20 = 300 ✓ 对齐！

      280 ┌────┐
          │    │
          │[车]│
      300 └────┘ ← 车头
       (停止线)
```

### 北向车辆 (↑)

```
停止线位置: stopLine.y = 300
车辆高度: height = 20

计算：
  车辆左上角Y = stopLine.y
  车辆左上角Y = 300

结果：
  车头（上方）= 300 ✓ 对齐！

   (停止线)
      300 ┌────┐ ← 车头
          │[车]│
          │    │
      320 └────┘
```

## 调整动画流程

```
步骤 1: 检测触发
─────────────────────
  车头距离停止线 <= 10px
  → shouldStopAtLine() 返回 true

步骤 2: 暂停动画
─────────────────────
  movementTimeline.pause()
  → 停止当前移动

步骤 3: 精确对齐
─────────────────────
  alignToStopLine() 被调用
  → 计算目标位置
  → GSAP 动画调整 (0.1秒)

步骤 4: 最终状态
─────────────────────
  车头精确对齐停止线
  isAtStopLine = true
  等待绿灯...
```

## 关键参数说明

| 参数 | 修复前 | 修复后 | 说明 |
|------|--------|--------|------|
| SENSITIVITY | 2px | 10px | 检测触发距离 |
| TARGET_POSITION.EAST | 5px | 0px | 东向目标偏移 |
| TARGET_POSITION.WEST | 5px | 0px | 西向目标偏移 |
| TARGET_POSITION.NORTH | 5px | 0px | 北向目标偏移 |
| TARGET_POSITION.SOUTH | 5px | 0px | 南向目标偏移 |
| 调整动画时长 | - | 0.1s | 平滑对齐时间 |

## 预期效果对比

### 修复前

```
车辆 1: ████      |  ← 停止线前 3px
车辆 2:   ██████  |  ← 停止线前 7px
车辆 3:     ████  |  ← 停止线前 2px
         (不对齐，参差不齐)
```

### 修复后

```
车辆 1: ████      |  ← 精确对齐
车辆 2:   ██████  |  ← 精确对齐
车辆 3:     ████  |  ← 精确对齐
         (完美对齐！)
```

## 技术亮点

1. **精确计算**
   - 考虑车辆尺寸（宽度/高度）
   - 考虑行驶方向（东西南北）
   - 计算车辆左上角应在位置

2. **平滑调整**
   - 使用 GSAP 动画库
   - 0.1秒过渡动画
   - power2.out 缓动效果

3. **可靠检测**
   - 10px 检测范围
   - 提前触发，有充足调整时间
   - 避免漏检

4. **代码复用**
   - 封装在 StopLineController
   - 统一处理所有方向
   - 易于维护和扩展

---

修复日期: 2025-01-03
版本: v1.0
