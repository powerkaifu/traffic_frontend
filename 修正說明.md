# 間距縮小功能修正說明

## 問題分析

### 原始問題
當車輛快速生成時，後車因碰撞檢測停在遠處，與前方紅燈排隊隊列形成大段空隙。

### 第一次修改的問題 ❌
修改太過激進，條件寬鬆（`minDistance <= QUEUE_GAP * 3`），導致：
- **破壞正常排隊**：已在停止線附近排隊的車輛也被觸發
- **車輛擠在一起**：正常排隊間距被破壞
- **影響範圍過大**：所有停車車輛都受影響

### 修正後的解決方案 ✅

## 精準的三重判斷條件

只有**同時滿足**以下三個條件，才會啟動縮小間距：

```javascript
// 條件 1：後車遠離停止線
const farFromStopLine = 後車距停止線 > 80px

// 條件 2：間距顯著過大  
const excessiveGap = 車間距 > QUEUE_GAP * 2  // 約 30-40px

// 條件 3：前車在排隊隊列
const frontVehicleNearStopLine = 前車距停止線 < 50px

// 三個條件同時滿足才執行
if (farFromStopLine && excessiveGap && frontVehicleNearStopLine) {
    啟動縮小間距
}
```

## 場景對比圖

### ✅ 正常排隊（不觸發）
```
停止線
  ↓
  🚦
  ↑
 [15px] ← 正常間距
  ↑
  🚗 車1 (距停止線 15px)
  ↑
 [15px] ← 正常間距
  ↑
  🚗 車2 (距停止線 30px)
  ↑
 [15px] ← 正常間距
  ↑
  🚗 車3 (距停止線 45px)
```
**結果**：所有車輛都在 80px 內，保持原有排隊機制 ✓

### ✅ 遠距離需縮小間距（觸發）
```
停止線
  ↓
  🚦
  ↑
 [15px]
  ↑
  🚗 車1 (距停止線 15px) ← 在排隊
  ↑
 [15px]
  ↑
  🚗 車2 (距停止線 30px) ← 在排隊
  ↑
 [80px] ← 過大間距！
  ↑
  🚗 車3 (距停止線 110px) ← 遠離停止線
```
**結果**：車3 啟動縮小間距，緩慢前進至合理距離 ✓

## 核心邏輯修改

### CollisionController.js

#### 1. performQueueingCollisionCheck() 方法
```javascript
// 🎯 新增：精準判斷邏輯
const myDistanceToStopLine = this.vehicle.getDistanceToStopLine()
const farFromStopLine = myDistanceToStopLine !== null && Math.abs(myDistanceToStopLine) > 80

const excessiveGap = minDistance > QUEUE_GAP * 2

const frontVehicleDistanceToStopLine = closestFrontVehicle.getDistanceToStopLine()
const frontVehicleNearStopLine = frontVehicleDistanceToStopLine !== null && 
                                  Math.abs(frontVehicleDistanceToStopLine) < 50

// 三重條件判斷
if (farFromStopLine && excessiveGap && frontVehicleNearStopLine) {
    // 啟動縮小間距
}
```

#### 2. checkSimpleCollision() 方法
```javascript
if (frontVehicleSpeed <= 0.1) {
    if (distance > effectiveStopDistance + 2) {
        // 🎯 檢查是否符合縮小間距條件
        const farFromStopLine = 後車距停止線 > 80px
        const frontNearStopLine = 前車距停止線 < 50px
        const significantGap = distance > effectiveStopDistance * 2
        
        if (farFromStopLine && frontNearStopLine && significantGap) {
            // 縮小間距速度（0.08-0.2）
        } else {
            // 正常排隊速度（0.12）
        }
    }
}
```

## 參數設定

| 參數 | 值 | 說明 |
|------|---|------|
| 遠離停止線閾值 | > 80px | 超過此距離才可能觸發縮小間距 |
| 前車在隊列閾值 | < 50px | 前車在此距離內視為在排隊 |
| 過大間距閾值 | > QUEUE_GAP * 2 | 約 30-40px，觸發縮小間距 |
| 縮小間距速度 | 0.08 - 0.2 | 保守速度，避免碰撞 |
| 正常排隊速度 | 0.12 | 不觸發縮小間距時使用 |

## 效果驗證

### ✅ 正確行為

1. **正常排隊不受影響**
   - 停止線 50px 內的車輛維持原有間距
   - 一台接一台正常排隊
   - 間距穩定在 QUEUE_GAP 左右

2. **遠距離車輛會縮小間距**
   - 距停止線 > 80px 的車輛
   - 與前車間距 > 40px 時
   - 緩慢前進縮小至合理距離

3. **安全性保證**
   - 最高速度限制在 0.2
   - 漸進式速度調整
   - 不會造成碰撞

### ❌ 避免的錯誤行為

1. ~~車輛在停止線附近還在移動~~（已修正）
2. ~~正常排隊間距被破壞~~（已修正）
3. ~~車輛擠在一起~~（已修正）

## 測試檢查清單

- [ ] 正常紅燈排隊：車輛間距穩定，不會緊縮
- [ ] 遠距離停車：會自動縮小間距加入隊列
- [ ] 燈號切換：車輛依序啟動，不會擠在一起
- [ ] 高密度車流：系統穩定運作
- [ ] 不同車道：1號車道和其他車道都正常

## 修改文件清單

1. ✅ `src/classes/vehicle_utils/CollisionController.js`
   - `performQueueingCollisionCheck()` - 精準三重判斷
   - `checkSimpleCollision()` - 位置感知的速度調整

2. ✅ `src/classes/Vehicle.js`
   - 處理 `gapClosing` 標記
   - `resumeMovement()` 支援縮小間距狀態

3. ✅ `GAP_CLOSING_SOLUTION.md` - 完整技術文檔
4. ✅ `修正說明.md` - 本文件

## 總結

此次修正採用**精準的三重條件判斷**，確保：
- ✅ **完全不影響正常排隊**（距停止線 < 80px）
- ✅ **只處理真正的問題**（遠距離 + 大間距 + 前車在隊列）
- ✅ **保守的速度控制**（最高 0.2，避免碰撞）
- ✅ **清晰的場景區分**（正常排隊 vs 縮小間距）

原本的排隊機制完全保留，只在真正需要時才啟動縮小間距功能！
