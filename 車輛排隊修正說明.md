# 🚗 車輛排隊重疊問題修正說明

## ❌ 問題描述

紅燈排隊的車輛會重疊在一起，沒有保持正常的安全間距。這是因為之前添加的「間距縮小」功能過於激進，破壞了原本正確的排隊機制。

## ✅ 解決方案

### 核心修正

1. **完全移除間距縮小邏輯**
   - 移除 `performQueueingCollisionCheck` 中的「情況4」
   - 移除 `checkSimpleCollision` 中的位置感知縮小間距
   - 移除 `Vehicle.js` 中的 `gapClosing` 狀態處理

2. **恢復原始排隊機制**
   - 車輛按照 `MIN_GAP`（25px）保持間距
   - 正常的碰撞檢測和停車邏輯
   - 一台接一台依序排隊

3. **使用配置文件參數**
   - 從 `vehicleConfig.js` 導入 `DISTANCE_CONFIG`
   - 使用 `BASE_DISTANCES.MIN_GAP`（25px）
   - 使用 `FOLLOWING_CONFIG.SPEED_RATIOS`

## 📋 修改內容

### 1. CollisionController.js - 排隊邏輯

```javascript
// ✅ 使用配置文件的排隊參數
const baseGap = DISTANCE_CONFIG.BASE_DISTANCES.MIN_GAP // 25px

// 根據車道和燈號狀態調整
if (this.vehicle.laneNumber === 1) {
  if (canProceedWithTrafficLight) {
    QUEUE_GAP = baseGap * 0.8 // 20px (左轉綠燈)
    MIN_FOLLOW_DISTANCE = baseGap * 0.5 // 12.5px
  } else {
    QUEUE_GAP = baseGap // 25px (紅燈)
    MIN_FOLLOW_DISTANCE = baseGap * 0.6 // 15px
  }
} else {
  QUEUE_GAP = baseGap // 25px (直行車道)
  MIN_FOLLOW_DISTANCE = baseGap * 0.5 // 12.5px
}
```

**三種排隊情況：**

1. **前車已通過停止線** → 允許繼續前進
2. **前車正在移動且距離合理** → 跟隨前進（速度 0.3-0.9）
3. **距離太近但前車停止** → 
   - 距離 > QUEUE_GAP * 0.3 → 慢速前進（0.12）
   - 距離 <= QUEUE_GAP * 0.3 → 完全停止

### 2. CollisionController.js - 簡單碰撞檢測

```javascript
// ✅ 前車停止時使用標準速度
if (frontVehicleSpeed <= 0.1) {
  if (distance > effectiveStopDistance + 2) {
    // 使用配置的最小跟車速度
    speedRatio = FOLLOWING_CONFIG.SPEED_RATIOS.MIN_SPEED_RATIO // 0.15
  } else {
    speedRatio = 0 // 距離太近停止
  }
}
```

### 3. Vehicle.js - 移除間距縮小

```javascript
// ✅ 恢復原始邏輯
else if (!shouldStop.frontVehicleIsMoving) {
  // 前方車輛停止且不移動：停止跟車
  this.movementTimeline.timeScale(0)
  this.currentState = 'stopped'
  return
}
```

## 🔧 配置參數使用

### vehicleConfig.js

```javascript
DISTANCE_CONFIG: {
  BASE_DISTANCES: {
    MIN_GAP: 25,              // ✅ 最小車輛間隙（排隊基準）
    SAFE_FOLLOWING: 35,       // 安全跟車距離
    EMERGENCY_STOP: 50,       // 緊急停車距離
  }
}

FOLLOWING_CONFIG: {
  SPEED_RATIOS: {
    MIN_SPEED_RATIO: 0.15,    // ✅ 最低速度比例（緩慢前進）
    MIN_ABSOLUTE_RATIO: 0.1,  // 絕對最低速度比例
  }
}

COLLISION_CONFIG: {
  DETECTION_DISTANCES: {
    STOP_DISTANCE: 12,        // ✅ 停止距離
    SLOW_DISTANCE: 25,        // ✅ 減速距離
  }
}
```

## 📊 排隊間距標準

| 車道 | 燈號狀態 | QUEUE_GAP | MIN_FOLLOW_DISTANCE |
|------|---------|-----------|---------------------|
| 1號車道 | 紅燈 | 25px | 15px |
| 1號車道 | 左轉綠燈 | 20px | 12.5px |
| 其他車道 | 任何 | 25px | 12.5px |

## ✅ 預期效果

1. **正常排隊**
   - 車輛間距保持 25px 左右
   - 一台接一台依序排隊
   - 不會重疊或碰撞

2. **平滑移動**
   - 前車移動時，後車以 0.3-0.9 的速度跟隨
   - 距離太近時自動減速或停止
   - 使用配置的最小速度（0.15）緩慢前進

3. **安全性**
   - 距離 < MIN_FOLLOW_DISTANCE 時完全停止
   - 使用配置的安全距離參數
   - 符合實際交通規則

## 🔄 修改文件

1. ✅ `src/classes/vehicle_utils/CollisionController.js`
   - 移除間距縮小邏輯
   - 使用配置參數（DISTANCE_CONFIG, FOLLOWING_CONFIG）
   - 恢復原始排隊機制

2. ✅ `src/classes/Vehicle.js`
   - 移除 `gapClosing` 狀態
   - 恢復原始停車邏輯
   - 移除 `resumeMovement` 中的間距縮小檢查

## 🧪 測試檢查

- [ ] 紅燈排隊：車輛間距約 25px，不重疊
- [ ] 連續到達：後車會在適當距離停止
- [ ] 綠燈啟動：車輛依序啟動，保持間距
- [ ] 不同車道：1號車道和其他車道都正常
- [ ] 無重疊：所有情況下車輛都不會重疊

## 📝 總結

此次修正**完全恢復原始的排隊機制**，並且**全面使用配置文件參數**：

- ❌ 移除所有間距縮小相關代碼
- ✅ 恢復基於 MIN_GAP (25px) 的標準排隊
- ✅ 使用 `DISTANCE_CONFIG.BASE_DISTANCES.MIN_GAP`
- ✅ 使用 `FOLLOWING_CONFIG.SPEED_RATIOS.MIN_SPEED_RATIO`
- ✅ 使用 `COLLISION_CONFIG.DETECTION_DISTANCES`
- ✅ 保持原有的碰撞檢測邏輯
- ✅ 確保車輛不會重疊

**不再使用硬編碼數值，所有參數都從配置文件讀取！**
