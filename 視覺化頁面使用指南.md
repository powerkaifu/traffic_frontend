# 交通數據視覺化頁面使用指南

## 功能概述

這個視覺化頁面提供了一個完整的交通數據分析儀表板，包含以下主要功能：

### 1. 數據查詢控制面板

- **日期範圍選擇器**: 支援範圍選擇和單日選擇
- **快速選擇按鈕**: 今天、一週、一個月的快速選項
- **VD 站點選擇**: 多選下拉選單，可選擇特定的 VD 站點
- **載入按鈕**: 根據選擇的條件載入數據

### 2. 三個分析標籤頁

#### 時間序列分析 (`/visualization/timeseries`)

- **功能**: 顯示交通燈秒數隨時間的變化趨勢
- **圖表類型**: D3.js 時間序列線圖
- **互動功能**:
  - 可點擊數據點查看詳細資訊
  - 可切換東西向/南北向燈號顯示
  - 滑鼠懸停顯示 tooltip
- **特色**: 雙線圖比較東西向和南北向燈號秒數

#### 關聯性分析 (`/visualization/correlation`)

- **功能**: 分析交通流量與燈號時間的關聯性
- **包含兩個圖表**:
  - **散點圖**: 顯示流量與燈號秒數的相關性
  - **熱力圖**: 按小時和 VD 站點展示平均流量分佈
- **互動功能**: 滑鼠懸停顯示詳細數值和站點資訊

#### 統計摘要 (`/visualization/summary`)

- **功能**: 提供數據的統計摘要和詳細表格
- **包含內容**:
  - 統計卡片: 平均東西向秒數、南北向秒數、總流量、平均車速
  - 詳細表格: 按 VD 站點分組的統計資料

### 3. 詳細資訊彈出視窗

- 點擊時間序列圖上的任意數據點
- 顯示該時刻的完整交通資訊
- 包含所有相關 VD 站點的詳細數據

## 技術特性

### 前端架構

- **框架**: Vue 3 + Quasar Framework
- **視覺化**: D3.js
- **路由**: Vue Router (支援子路由)
- **狀態管理**: Vue 3 Composition API
- **UI 組件**: Quasar Components

### API 設計

- **API 模組**: `src/api/index.js`
- **支援功能**:
  - 日期範圍查詢
  - VD 站點篩選
  - 分頁和限制
  - 模擬數據生成器 (開發階段)

### 響應式設計

- 支援桌面和行動裝置
- 自適應網格佈局
- 觸控友好的互動元素

## 使用方式

### 基本流程

1. **進入頁面**: 訪問 `/visualization` 路由
2. **選擇數據範圍**: 使用日期選擇器或快速選擇按鈕
3. **選擇 VD 站點**: 從下拉選單中選擇要分析的站點
4. **載入數據**: 點擊「載入數據」按鈕
5. **分析數據**: 在三個標籤頁間切換進行不同類型的分析

### 預設行為

- 進入頁面時自動載入最近一週的數據
- 預設選擇所有可用的 VD 站點
- 預設顯示時間序列分析頁面

### 互動操作

- **圖表縮放**: 滑鼠滾輪縮放（部分圖表支援）
- **詳細資訊**: 點擊數據點查看詳細資訊
- **tooltip**: 滑鼠懸停顯示即時數值
- **圖表設定**: 可切換顯示的數據系列

## 數據格式

### API 響應格式

```json
{
  "query_info": {
    "period": "2024-01-01 ~ 2024-01-31",
    "data_points": 168
  },
  "data": [
    {
      "group": {
        "group_id": "123e4567-e89b-12d3-a456-426614174000",
        "timestamp": "2024-01-01T08:30:00Z",
        "east_west_seconds": 65,
        "south_north_seconds": 58
      },
      "intersections": [
        {
          "id": 1,
          "VD_ID": "VLRJX20",
          "DayOfWeek": 1,
          "Hour": 8,
          "IsPeakHour": true,
          "Speed": 42.5,
          "Occupancy": 15.3,
          "Volume_M": 100,
          "total_volume": 205,
          "created_at": "2024-01-01T08:30:00Z"
        }
      ]
    }
  ]
}
```

## 開發和部署

### 開發環境

```bash
# 啟動開發伺服器
npm run dev

# 訪問頁面
http://localhost:9001/visualization
```

### 生產部署

```bash
# 建置專案
npm run build

# 部署 dist 資料夾到伺服器
```

### 自定義配置

- API 端點配置在 `src/api/index.js`
- 主題和樣式配置在各組件的 `<style>` 區塊
- 路由配置在 `src/router/routes.js`

## 擴展功能

### 新增圖表類型

1. 在相應的標籤頁區域新增圖表容器
2. 實作 D3.js 繪製函數
3. 在 `updateCharts()` 方法中加入調用

### 新增 API 端點

1. 在 `src/api/index.js` 中新增 API 方法
2. 在組件中導入並使用

### 新增子路由

1. 在 `src/router/routes.js` 中新增路由定義
2. 在 `VisualizationPage.vue` 中新增對應的標籤頁和內容

## 注意事項

- 目前使用模擬數據，實際部署時需要連接真實的後端 API
- 建議在生產環境中加入錯誤處理和載入狀態管理
- 大量數據時建議加入虛擬滾動或分頁功能
- 建議加入數據快取機制以提升使用者體驗
