# 🚗 碰撞後自動跟隨功能說明

## 📋 功能概述

實施了**碰撞後自動跟隨前車**的功能，讓後車在碰撞停止後，能自動緩慢跟隨前車到合適的跟車距離，解決後方快速追撞車輛停在遠處的問題。

---

## 🎯 核心原理

### 工作流程

```
1. 車輛快速接近前車
   ↓
2. 距離 <= 12px 觸發碰撞檢測
   ↓
3. 檢測前車是否停止
   ↓ 是
4. 檢查距離 > 最小跟隨距離（8px）
   ↓ 是
5. 根據距離選擇跟隨速度
   ↓
6. 設定 autoFollowing 狀態和速度
   ↓
7. 車輛緩慢跟隨前車
   ↓
8. 持續監控距離（每 50ms）
   ↓
9. 到達目標距離（25px）時減速
   ↓
10. 到達最小距離（8px）時停止
```

### 距離分層

| 距離範圍 | 跟隨速度 | 說明 |
|---------|---------|------|
| < 8px | 0（停止） | 最小安全距離 |
| 8-15px | 0.05（5%） | 非常接近，微調速度 |
| 15-25px | 0.12（12%） | 接近目標，慢速靠近 |
| 25-35px | 0.18（18%） | 一般距離，正常跟隨 |
| 35-50px | 0.25（25%） | 較遠距離，快速跟隨 |
| > 50px | 不跟隨 | 距離太遠，正常移動 |

---

## 🔧 配置參數

### vehicleConfig.js - 新增配置

```javascript
FOLLOWING_CONFIG: {
  // 🚗 碰撞後自動跟隨設定
  AUTO_FOLLOW_AFTER_COLLISION: {
    ENABLED: true, // 啟用碰撞後自動跟隨
    MIN_FOLLOW_DISTANCE: 8, // 最小跟隨距離（px）
    TARGET_FOLLOW_DISTANCE: 25, // 目標跟隨距離（px）
    MAX_FOLLOW_DISTANCE: 50, // 最大跟隨距離（px）
    
    // 跟隨速度設定
    FOLLOW_SPEEDS: {
      VERY_CLOSE: 0.05, // 8-15px
      CLOSE: 0.12, // 15-25px
      NORMAL: 0.18, // 25-35px
      FAR: 0.25, // 35-50px
    },
    
    // 距離閾值
    DISTANCE_THRESHOLDS: {
      VERY_CLOSE: 15,
      CLOSE: 25,
      NORMAL: 35,
    },
  },
}
```

---

## 📂 修改的文件

### 1. vehicleConfig.js
**新增**：`AUTO_FOLLOW_AFTER_COLLISION` 配置區塊

```javascript
// 新增完整的自動跟隨配置
FOLLOWING_CONFIG: {
  AUTO_FOLLOW_AFTER_COLLISION: {
    ENABLED: true,
    MIN_FOLLOW_DISTANCE: 8,
    TARGET_FOLLOW_DISTANCE: 25,
    // ...
  }
}
```

### 2. CollisionController.js
**修改**：`checkSimpleCollision` 方法（第 726-771 行）

```javascript
// 在距離 <= effectiveStopDistance 時
if (distance <= effectiveStopDistance) {
  const autoFollowConfig = FOLLOWING_CONFIG.AUTO_FOLLOW_AFTER_COLLISION
  
  if (autoFollowConfig.ENABLED) {
    const frontIsStopped = frontVehicleSpeed <= 0.1
    
    if (frontIsStopped && distance > autoFollowConfig.MIN_FOLLOW_DISTANCE) {
      // 根據距離選擇跟隨速度
      let followSpeed = 計算速度()
      
      return {
        autoFollowing: true,
        targetSpeed: followSpeed,
        reason: '自動跟隨前車'
      }
    }
  }
  
  // 原始停止邏輯
  return { shouldStop: true, targetSpeed: 0 }
}
```

### 3. Vehicle.js
**修改 1**：優先處理自動跟隨（第 1070-1079 行）

```javascript
// 優先處理自動跟隨模式
if (shouldStop && shouldStop.autoFollowing && shouldStop.targetSpeed > 0) {
  gsap.to(this.movementTimeline, {
    timeScale: shouldStop.targetSpeed,
    duration: ANIMATION_CONFIG.SPEED_CHANGE_DURATION.NORMAL,
    ease: 'power2.out',
  })
  this.currentState = 'autoFollowing'
  return
}
```

**修改 2**：resumeMovement 支援自動跟隨（第 768-771 行）

```javascript
if (
  this.movementTimeline &&
  (this.currentState === 'waiting' || 
   this.currentState === 'waitingForVehicle' || 
   this.currentState === 'slowing' ||
   this.currentState === 'autoFollowing') // 新增
) {
  // 檢查並處理自動跟隨
  if (collision.autoFollowing && collision.targetSpeed > 0) {
    設定跟隨速度
    this.currentState = 'autoFollowing'
    return
  }
}
```

**修改 3**：定期檢查包含自動跟隨（第 1027, 1221 行）

```javascript
if (this.currentState === 'waitingForVehicle' || 
    this.currentState === 'autoFollowing') {
  this.resumeMovement(allVehicles)
}
```

---

## 🔍 工作場景

### ✅ 場景 1：紅燈排隊（正常運作）

```
停止線
  ↓
  🚦
  ↑ 25px
  🚗 車1 (停止)
  ↑ 25px
  🚗 車2 (停止)
  ↑ 25px
  🚗 車3 (停止)
```
- 車輛間距穩定在 25px
- 不啟動自動跟隨（距離已在目標範圍）

### ✅ 場景 2：快速追撞後自動跟隨

```
初始狀態：
停止線 ← [25px] ← 車1 ← [40px] ← 車2 (快速接近)
                   停止          

追撞停止：
停止線 ← [25px] ← 車1 ← [10px] ← 車2 (碰撞停止)
                   停止          距離 < 12px

自動跟隨：
停止線 ← [25px] ← 車1 ← [12px] ← 車2 (跟隨中)
                   停止          速度 12%

到達目標：
停止線 ← [25px] ← 車1 ← [25px] ← 車2 (到位停止)
                   停止          速度 0%
```

### ✅ 場景 3：前車移動時跟隨

```
前車開始移動：
停止線 ← [30px] ← 車1 (移動中) ← [25px] ← 車2 (跟隨)
                  速度 50%              速度 18%

持續跟隨：
停止線 ← [35px] ← 車1 (移動中) ← [25px] ← 車2 (跟隨)
                  速度 50%              速度 18%
```

---

## 🧪 測試驗證

### 方法 1：Console 監控

```javascript
// 監控自動跟隨狀態
setInterval(() => {
  const following = window.liveVehicles?.filter(v => v.currentState === 'autoFollowing') || []
  
  if (following.length > 0) {
    console.log('🚗 自動跟隨中的車輛:')
    following.forEach(v => {
      const speed = v.movementTimeline?.timeScale()
      console.log(`  ${v.id}: 速度 ${(speed*100).toFixed(0)}%`)
    })
  }
}, 1000)
```

### 方法 2：檢查特定車輛

```javascript
const vehicle = window.liveVehicles[0]
console.log('狀態:', vehicle.currentState)
console.log('速度:', vehicle.movementTimeline?.timeScale())
```

### 方法 3：調試輸出

在 CollisionController.js 第 753 行加入：

```javascript
if (followSpeed > 0) {
  console.log('🚗 自動跟隨啟動:', {
    車輛: this.vehicle.id,
    距離: distance.toFixed(1) + 'px',
    速度: (followSpeed * 100).toFixed(0) + '%'
  })
  return { ... }
}
```

---

## 📊 參數調整

### 如果跟隨太慢

```javascript
// vehicleConfig.js
FOLLOW_SPEEDS: {
  VERY_CLOSE: 0.08, // 從 0.05 提高
  CLOSE: 0.18, // 從 0.12 提高
  NORMAL: 0.25, // 從 0.18 提高
  FAR: 0.35, // 從 0.25 提高
}
```

### 如果距離太近

```javascript
// vehicleConfig.js
MIN_FOLLOW_DISTANCE: 12, // 從 8 提高到 12
TARGET_FOLLOW_DISTANCE: 30, // 從 25 提高到 30
```

### 如果觸發太少

```javascript
// vehicleConfig.js
MAX_FOLLOW_DISTANCE: 80, // 從 50 提高到 80
```

### 如果要禁用功能

```javascript
// vehicleConfig.js
AUTO_FOLLOW_AFTER_COLLISION: {
  ENABLED: false, // 設為 false
}
```

---

## ✅ 優點

1. **自動化**：無需手動操作，自動跟隨前車
2. **平滑**：速度根據距離動態調整，移動流暢
3. **安全**：保持最小安全距離（8px）
4. **配置化**：所有參數都可調整
5. **兼容性**：不影響原有的紅燈排隊機制

---

## 📝 注意事項

### 與紅燈排隊的區別

| 項目 | 紅燈排隊 | 自動跟隨 |
|------|---------|---------|
| 觸發條件 | 接近停止線 | 碰撞停止 |
| 目標距離 | 25px | 25px |
| 最小距離 | 約 15px | 8px |
| 速度範圍 | 0.12-0.3 | 0.05-0.25 |
| 狀態標記 | 無 | autoFollowing |

### 工作配合

- **紅燈排隊**：在停止線附近，車輛依序排隊
- **自動跟隨**：碰撞後，車輛自動靠近前車
- **互不干擾**：兩個機制獨立運作

---

## 🎉 預期效果

實施後應該看到：

1. ✅ 後車快速追撞前車後，不會停在遠處
2. ✅ 車輛會自動緩慢跟隨前車
3. ✅ 最終停在距離前車約 25px 的位置
4. ✅ 跟隨速度根據距離自動調整（5%-25%）
5. ✅ 不影響停止線附近的正常排隊
6. ✅ 車輛狀態為 `autoFollowing` 時可見跟隨行為

---

**現在碰撞後的車輛會自動跟隨前車，不再停在遠處了！** 🚀
