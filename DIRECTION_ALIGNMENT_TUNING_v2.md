# 🎯 方向特定停止線對齁調整指南 (v2 - 已修復)

## 📊 問題現象

根據實測數據，每個方向的首車距停止線誤差：

- 🔴 **EAST**: 6.72px (應該是 0px)
- 🔵 **WEST**: 7.82px (應該是 0px)
- 🟡 **NORTH**: 4.71px (應該是 0px)
- 🟢 **SOUTH**: 9.06px (應該是 0px)

**根本原因**: `_calculateDistanceToStopLine()` 計算中包含了 `effectiveOffset`，導致測量值總是偏高。

## ✅ 已修復

已修改 `_calculateDistanceToStopLine()` 方法：

- ✅ 移除計算中的 `effectiveOffset` 成分
- ✅ 現在只計算「原始距離」（停止線位置 - 車頭位置）
- ✅ 偏移調整在 `checkSimpleCollision()` 中單獨進行

## 🎯 新的診斷流程

### 方式 1: 自動診斷（推薦）✨

1. **編譯完成** (`npm run build`)
2. **刷新瀏覽器**
3. **等待 30 秒**自動完成診斷
4. **查看控制台輸出**（Console）
5. **複製建議的調整值**

診斷工具會自動：

- ✅ 收集每個方向的首車最小距離
- ✅ 計算每個方向應該調整的偏移值
- ✅ 提供配置更新建議

### 方式 2: 手動診斷

在瀏覽器控制台執行：

```javascript
// 啟動診斷（監控 30 秒）
window.preciseAlignmentDiagnostic.start()

// 提前停止（如需）
window.preciseAlignmentDiagnostic.stop()
```

## 📋 診斷報告示例

```
═══════════════════════════════════════════════════════
🎯 精準對齁診斷報告 (30秒監控結果)
═══════════════════════════════════════════════════════

🔴 東向
  ├─ 首車最小距離: 6.72px
  ├─ 停止次數: 5
  ├─ 所有停止車平均距離: 45.33px (樣本: 15)
  └─ 💡 建議調整: 🔴 東向 → -7px (停在停止線前 6.72px)

🔵 西向
  ├─ 首車最小距離: 7.82px
  ├─ 停止次數: 4
  ├─ 所有停止車平均距離: 189.45px (樣本: 12)
  └─ 💡 建議調整: 🔵 西向 → -8px (停在停止線前 7.82px)

🟡 北向
  ├─ 首車最小距離: 4.71px
  ├─ 停止次數: 6
  ├─ 所有停止車平均距離: 52.18px (樣本: 18)
  └─ 💡 建議調整: 🟡 北向 → -5px (停在停止線前 4.71px)

🟢 南向
  ├─ 首車最小距離: 9.06px
  ├─ 停止次數: 5
  ├─ 所有停止車平均距離: 48.92px (樣本: 16)
  └─ 💡 建議調整: 🟢 南向 → -9px (停在停止線前 9.06px)

═══════════════════════════════════════════════════════
📝 建議的配置更新:
═══════════════════════════════════════════════════════

將以下值複製到 CollisionController.js:

STOP_LINE_OFFSET_BY_DIRECTION: {
  east: -7,
  west: -8,
  north: -5,
  south: -9,
},
```

## 🔧 調整步驟

### 步驟 1: 重新編譯

```bash
npm run build
```

### 步驟 2: 自動診斷

1. 刷新瀏覽器
2. 等待 30 秒（診斷工具自動運行）
3. 查看控制台報告

### 步驟 3: 複製建議值

報告會自動提供調整值，例如：

```javascript
STOP_LINE_OFFSET_BY_DIRECTION: {
  east: -7,    // 向後調整 7px
  west: -8,    // 向後調整 8px
  north: -5,   // 向後調整 5px
  south: -9,   // 向後調整 9px
},
```

### 步驟 4: 更新配置

編輯 `src/classes/vehicle_utils/CollisionController.js` 第 16-23 行，複製診斷工具提供的值。

### 步驟 5: 再次編譯

```bash
npm run build
```

### 步驟 6: 驗證

刷新瀏覽器，重新執行診斷，確認首車距離停止線 ≈ 0 ± 0.5px

## 💡 調整值的含義

| 符號          | 含義                | 示例                  |
| ------------- | ------------------- | --------------------- |
| **正值** `+N` | 停止線向前移動 N px | `+5` = 停止線提前 5px |
| **負值** `-N` | 停止線向後移動 N px | `-7` = 停止線延遲 7px |
| **0**         | 無調整              | `0` = 停止線不移動    |

## 🔍 診斷工具原理

### 收集的數據

- **首車最小距離**: 同方向停止車中距離停止線最近的距離
- **停止次數**: 該方向首車停止的次數
- **平均距離**: 該方向所有停止車的平均距離（包括排隊車輛）

### 為什麼平均距離這麼大？

**答：** 只有首車停在停止線附近，後續車輛被前車碰撞檢測擋住，停在距離停止線很遠的地方。

例如：

- 🔴 東向首車停在 6.72px → 精準對齁 ✓
- 🔴 東向第 2 台車停在 43.56px → 被首車擋住（碰撞檢測）
- 🔴 東向第 3 台車停在 85.14px → 被第 2 台車擋住

**所以只需關注「首車最小距離」，這才是精準對齁的指標。**

## ✨ 最終成果

完成調整後，預期結果：

- ✅ 每個方向的首車都精準停在停止線上（距離 ≈ 0 ± 0.5px）
- ✅ 後續車輛按照 TARGET_SPACING 間距排隊
- ✅ 四個方向都對齁

## 📝 下一步

請按照以下步驟執行：

1. **運行編譯**

   ```bash
   npm run build
   ```

2. **刷新瀏覽器** 並等待 30 秒

3. **查看控制台** (按 F12 打開)

4. **複製診斷建議**

5. **更新配置檔案** `src/classes/vehicle_utils/CollisionController.js`

6. **重新編譯並驗證**

完成後，告訴我診斷報告的結果，我會幫你驗證是否需要進一步調整！

---

**最後更新**: 2025-11-11
**狀態**: 🟢 修復完成，等待用户執行新診斷
