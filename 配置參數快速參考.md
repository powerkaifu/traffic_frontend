# 🔧 配置參數快速參考

本專案所有參數都集中在配置文件中，不使用硬編碼。

## 📂 配置文件位置

```
src/classes/config/
├── vehicleConfig.js      ← 車輛行為、碰撞、跟車參數
├── stopLineConfig.js     ← 停止線檢測參數
├── trafficConfig.js      ← 交通燈、速度、道路參數
├── trafficScenarioConfig.js
├── vdBasedTrafficConfig.js
└── weatherConfig.js
```

## 🚗 車輛排隊相關參數

### vehicleConfig.js - DISTANCE_CONFIG

```javascript
BASE_DISTANCES: {
  MIN_GAP: 25,              // 最小車輛間隙（排隊基準）✅
  SAFE_FOLLOWING: 35,       // 安全跟車距離
  EMERGENCY_STOP: 50,       // 緊急停車距離
  REQUIRED_SAFETY: 20,      // 基礎安全距離
}
```

### vehicleConfig.js - COLLISION_CONFIG

```javascript
DETECTION_DISTANCES: {
  STOP_DISTANCE: 12,        // 停止距離 ✅
  SLOW_DISTANCE: 25,        // 減速距離 ✅
  LANE_TOLERANCE: 25,       // 車道對齊容差
}

CHECK_INTERVAL: 100,         // 碰撞檢查間隔（毫秒）
SIMPLE_CHECK_INTERVAL: 50,   // 簡單碰撞檢查間隔
```

### vehicleConfig.js - FOLLOWING_CONFIG

```javascript
SPEED_RATIOS: {
  MIN_SPEED_RATIO: 0.15,    // 最低速度比例（緩慢前進）✅
  MIN_ABSOLUTE_RATIO: 0.1,  // 絕對最低速度比例
}

GREEN_LIGHT_FOLLOWING: {
  LANE1: {
    VERY_CLOSE: 0.15,       // 1號車道非常接近時速度
    CLOSE: 0.4,
    NORMAL: 0.7,
    FAR: 1.0,
  },
  OTHER_LANES: {
    VERY_CLOSE: 0.2,        // 其他車道非常接近時速度
    CLOSE: 0.5,
    NORMAL: 0.8,
    FAR: 1.0,
  }
}
```

## 🚦 停止線相關參數

### stopLineConfig.js

```javascript
DETECTION: {
  SENSITIVITY: 10,          // 停止線前觸發檢測距離
  PROXIMITY_RANGE: 50,      // 停止線附近區域範圍
}

TARGET_POSITION: {
  EAST: -5,                 // 東向停車位置（px）
  WEST: -8,
  NORTH: -10,
  SOUTH: -10,
}

TRAFFIC_LIGHT: {
  QUEUE_DISTANCE: 10,       // 排隊判斷距離
  APPROACH_DISTANCE: 5,     // 接近停車判斷距離
}
```

## 🎬 動畫相關參數

### vehicleConfig.js - ANIMATION_CONFIG

```javascript
TIME_MULTIPLIER: 0.5,       // 全域動畫速度（0.5=2倍速）

SPEED_CHANGE_DURATION: {
  INSTANT: 0.05,            // 幾乎立即（紅燈停車）
  FAST: 0.2,                // 快速響應（綠燈啟動）
  NORMAL: 0.3,              // 一般速度變化
  SMOOTH: 0.5,              // 平滑過渡
}

COOLDOWN_TIMES: {
  GLOBAL_ANTI_SHAKE: 100,   // 全域抖動防護冷卻
  POSITION_ADJUST: 500,     // 位置調整冷卻
}
```

## 🚦 交通燈相關參數

### trafficConfig.js - lightConfig

```javascript
normalCycle: {
  red: 30000,               // 紅燈 30 秒
  yellow: 3000,             // 黃燈 3 秒
  green: 25000,             // 綠燈 25 秒
  allRed: 2000,             // 全紅燈 2 秒
}
```

### trafficConfig.js - speedConfig

```javascript
speedConfig: {
  large: { min: 25, max: 35 },   // 大型車速度範圍
  small: { min: 30, max: 50 },   // 小型車速度範圍
  motor: { min: 30, max: 60 },   // 機車速度範圍
}
```

## 📊 當前排隊機制參數

### CollisionController.js 使用的參數

```javascript
// 基礎間距（從配置讀取）
const baseGap = DISTANCE_CONFIG.BASE_DISTANCES.MIN_GAP // 25px

// 根據車道和燈號調整
1號車道紅燈:
  QUEUE_GAP = baseGap = 25px
  MIN_FOLLOW_DISTANCE = baseGap * 0.6 = 15px

1號車道左轉綠燈:
  QUEUE_GAP = baseGap * 0.8 = 20px
  MIN_FOLLOW_DISTANCE = baseGap * 0.5 = 12.5px

其他車道:
  QUEUE_GAP = baseGap = 25px
  MIN_FOLLOW_DISTANCE = baseGap * 0.5 = 12.5px
```

### 排隊速度控制

```javascript
// 前車移動時的跟隨速度（0.3-0.9）
targetSpeed = Math.min(0.9, Math.max(0.3, minDistance / QUEUE_GAP))

// 前車停止時的緩慢前進速度
if (distance > QUEUE_GAP * 0.3) {
  targetSpeed = 0.12
} else {
  targetSpeed = 0  // 完全停止
}

// 簡單碰撞檢測中的最小速度
speedRatio = FOLLOWING_CONFIG.SPEED_RATIOS.MIN_SPEED_RATIO // 0.15
```

## 🔍 如何調整參數

### 1. 修改排隊間距
編輯 `vehicleConfig.js`:
```javascript
BASE_DISTANCES: {
  MIN_GAP: 30,  // 從 25 改為 30，增加排隊間距
}
```

### 2. 修改停車速度
編輯 `vehicleConfig.js`:
```javascript
SPEED_RATIOS: {
  MIN_SPEED_RATIO: 0.2,  // 從 0.15 改為 0.2，加快緩慢前進速度
}
```

### 3. 修改碰撞檢測距離
編輯 `vehicleConfig.js`:
```javascript
DETECTION_DISTANCES: {
  STOP_DISTANCE: 15,  // 從 12 改為 15，提前停止
  SLOW_DISTANCE: 30,  // 從 25 改為 30，提前減速
}
```

## ⚠️ 重要原則

1. **不要硬編碼**：所有數值都應該從配置文件讀取
2. **保持一致性**：相關參數應該在同一個配置區塊
3. **註解說明**：修改參數時要更新註解
4. **測試驗證**：修改後要測試所有場景

## 📝 參數命名規範

- 距離相關：`*_DISTANCE`, `*_GAP`, `*_RANGE`（單位：px）
- 時間相關：`*_DURATION`, `*_INTERVAL`, `*_TIME`（單位：ms 或 s）
- 速度相關：`*_SPEED`, `*_RATIO`, `*_MULTIPLIER`（比例值：0-1）
- 開關相關：`ENABLED`, `SHOW_*`, `USE_*`（布林值）
