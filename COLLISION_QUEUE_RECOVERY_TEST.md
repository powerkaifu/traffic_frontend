# 🧪 碰撞重新加入隊列 - 快速測試檢查單

## ⚡ 快速測試（5分鐘）

### 測試 1：單車碰撞融入隊列

**準備**:

```
1. 開啟應用程序
2. 選擇 "Peak Hour" 模式
3. 等待 10 秒形成排隊隊伍
```

**執行**:

```
1. 暫停自動生成 (關閉 Auto-Generate 開關)
2. 手動產生 1 台車到行進中的隊伍前方
3. 觀察車輛行為 20-30 秒
```

**驗證**:

- [ ] 車輛與前車碰撞時停止
- [ ] 停止 1-2 秒後開始向前移動
- [ ] 以 0.2-0.3 的速度朝向隊伍融入
- [ ] 最終融入隊伍，與隊伍同步移動
- [ ] **不會**永久停留在原地

**期望結果**:

```
✅ 進度: 停止 → 融入 → 同步 (3-5 秒內完成)
❌ 錯誤: 停止 → 永遠停止 (修復前的行為)
```

---

### 測試 2：多車連鎖融入

**準備**:

```
1. 選擇 "Peak Hour" 模式
2. 設置 100+ 車輛，等待 15 秒
3. 確保形成完整排隊隊伍
```

**執行**:

```
1. 關閉自動生成
2. 手動產生 3-5 台車進入隊伍中間
3. 觀察所有碰撞車輛的行為
```

**驗證**:

- [ ] 所有碰撞車輛都停止
- [ ] 所有停止車輛都逐一開始融入
- [ ] 融入順序合理 (先停的先開始移動)
- [ ] 最終形成連續隊伍
- [ ] 無永久停止的車輛

**期望結果**:

```
✅ 所有 3-5 台車都成功融入
❌ 部分車輛卡住不動
```

---

### 測試 3：紅燈排隊停止前碰撞

**準備**:

```
1. 選擇 Normal 模式
2. 觀察紅燈時形成的隊伍
3. 確保至少 5 台車排隊
```

**執行**:

```
1. 等待綠燈轉紅燈
2. 此時手動產生 1 台快速移動的車進入隊伍前方
3. 觀察 30 秒
```

**驗證**:

- [ ] 新車與隊伍碰撞，停止
- [ ] 停止 1-2 秒後開始融入
- [ ] 融入後與隊伍等待綠燈
- [ ] 綠燈時與隊伍一同通過

**期望結果**:

```
✅ 完整流程：碰撞 → 停止 → 融入 → 等待 → 通過
❌ 卡在碰撞點不動
```

---

## 📊 數據驗證（控制台測試）

### 檢查 1：確認修復代碼存在

**在瀏覽器控制台執行**:

```javascript
// 驗證 performMinimumGapCheck 修復
const code = `
  const otherSpeed = other.movementTimeline ? other.movementTimeline.timeScale() : 0
  if (otherSpeed <= 0.15) {
    return null
  }
`
console.log('✅ 修復代碼已存在') // 手動確認
```

### 檢查 2：監控融入隊列動作

**在瀏覽器控制台執行**:

```javascript
// 添加到 Vehicle.js onUpdate 後執行
setInterval(() => {
  const v = window.liveVehicles?.[0]
  if (v && v.currentState === 'rejoiningQueue') {
    console.log(`🚗 [${v.id}] 正在融入隊列，速度: ${(v.movementTimeline?.timeScale() * 100).toFixed(0)}%`)
  }
}, 1000)
```

### 檢查 3：隊伍距離檢查

**在瀏覽器控制台執行**:

```javascript
// 監控隊伍車輛距離
setInterval(() => {
  const queue = window.liveVehicles?.filter((v) => v.currentState === 'following' || v.currentState === 'stopped') || []
  if (queue.length >= 2) {
    queue.sort((a, b) => {
      const aPos = a.getCurrentPosition?.()?.x ?? 0
      const bPos = b.getCurrentPosition?.()?.x ?? 0
      return aPos - bPos
    })

    let distances = []
    for (let i = 0; i < queue.length - 1; i++) {
      const pos1 = queue[i].getCurrentPosition?.()
      const pos2 = queue[i + 1].getCurrentPosition?.()
      if (pos1 && pos2) {
        const dist = Math.abs(pos1.x - pos2.x)
        distances.push(dist.toFixed(1))
      }
    }
    console.log(`📏 隊伍距離: ${distances.join(' → ')} px`)
  }
}, 3000)
```

---

## 🐛 故障排除

### 問題 1：碰撞後仍永久停止

```
原因: 修復代碼未生效或加載舊版本
解決:
1. 清空瀏覽器快取 (Ctrl+Shift+Delete)
2. 強制刷新頁面 (Ctrl+F5)
3. 檢查主控台是否有 JS 錯誤
```

### 問題 2：融入速度過慢

```
原因: 融入隊列速度參數過低
解決:
1. 編輯 CollisionController.js (~line 878)
2. 增加 targetSpeed 值 (0.2 → 0.3 或更高)
3. 重新編譯並刷新
```

### 問題 3：無法識別隊伍

```
原因: findQueueTailVehicle 未找到隊伍
解決:
1. 確保隊伍中有停止或減速的車輛
2. 確保碰撞車與隊伍在同方向同車道
3. 檢查搜尋範圍是否足夠 (400px)
```

### 問題 4：融入後又停止

```
原因: 碰撞檢測仍在觸發
解決:
1. 檢查最小間距保護是否過度
2. 檢查是否有其他碰撞觸發
3. 增加 ABSOLUTE_MIN_GAP 範圍
```

---

## ✅ 完整測試清單

### 基礎功能 (必須通過)

- [ ] 碰撞停止正常運作
- [ ] 碰撞後車輛開始向隊伍移動 (2-5秒內)
- [ ] 融入速度在 0.05-0.6 之間
- [ ] 融入後與隊伍同步
- [ ] 無新增編譯錯誤

### 極端情況 (應通過)

- [ ] 5+ 台車連續碰撞都能融入
- [ ] 近距離碰撞 (<10px) 正常恢復
- [ ] 隊伍為空時碰撞自動減速
- [ ] 綠燈切換時碰撞車正常通過

### 性能要求 (必須滿足)

- [ ] CPU 占用無明顯增加
- [ ] FPS 保持 45+
- [ ] 融入時間 < 10 秒
- [ ] 無記憶體洩漏

### 品質指標 (建議)

- [ ] 隊伍距離保持 15-20px
- [ ] 無重疊 (距離 < 0)
- [ ] 停止→融入成功率 > 90%
- [ ] 排隊整齊度提升

---

## 📝 測試報告模板

```
# 碰撞重新加入隊列修復 - 測試報告

## 環境
- 日期: [日期]
- 瀏覽器: [Chrome/Firefox/等]
- 車輛數: [數量]
- 模式: [Peak Hour/Normal/等]

## 測試結果

### 測試 1：單車碰撞融入
- 狀態: ✅ 通過 / ⚠️ 部分通過 / ❌ 失敗
- 融入時間: [秒]
- 備註: [問題描述]

### 測試 2：多車連鎖融入
- 狀態: ✅ 通過 / ⚠️ 部分通過 / ❌ 失敗
- 融入數量: [數量]
- 備註: [問題描述]

### 測試 3：紅燈停止前碰撞
- 狀態: ✅ 通過 / ⚠️ 部分通過 / ❌ 失敗
- 備註: [問題描述]

## 性能數據
- CPU 占用: [%]
- FPS: [數值]
- 記憶體: [MB]

## 結論
[總體評估]
```

---

## 🎯 成功標準

**修復成功** =

```
✅ 碰撞停止 (0.5-1秒)
AND
✅ 開始融入 (1-2秒後)
AND
✅ 成功融入隊伍 (3-5秒內完成)
AND
✅ 與隊伍同步移動
AND
✅ 無永久停止
```

**修復失敗** =

```
❌ 碰撞停止但無法融入
OR
❌ 停止 > 5秒仍不移動
OR
❌ 新增編譯錯誤
```

---

**開始測試**: 現在可以進行完整測試了！
**預期時間**: 15-20 分鐘（完整驗證）
**快速驗證**: 5 分鐘（基本功能）
