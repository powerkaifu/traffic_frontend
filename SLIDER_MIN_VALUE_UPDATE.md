# 🎚️ 生成間隔拉桿最低值調整

## 📋 修改摘要

已調整生成間隔拉桿的最低秒數從 **1 秒** 改為 **2 秒**，防止使用者操作過度導致車流過多。

---

## ✅ 完成的修改

### 修改 1️⃣：拉桿 HTML 屬性

**位置**: `src/layouts/MainLayout.vue` Line ~179

**改變**:

```vue
<!-- ❌ 修改前 -->
<input type="range" v-model="manualInterval" min="1000" max="30000" :step="100" />

<!-- ✅ 修改後 -->
<input type="range" v-model="manualInterval" min="2000" max="30000" :step="100" />
```

**效果**: 拉桿無法調低於 2000ms (2秒)

---

### 修改 2️⃣：初始化值

**位置**: `src/layouts/MainLayout.vue` Line ~548

**改變**:

```javascript
// ❌ 修改前
const manualInterval = ref(1000)
const currentInterval = ref(1.0)

// ✅ 修改後
const manualInterval = ref(2000) // ✅ 改為 2000ms (2秒)
const currentInterval = ref(2.0) // ✅ 初始化為 2 秒
```

**效果**: 應用啟動時拉桿預設值為 2 秒

---

## 📊 修改前後對比

| 項目         | 修改前      | 修改後      | 說明                 |
| ------------ | ----------- | ----------- | -------------------- |
| **最低值**   | 1000ms      | 2000ms      | 最低生成間隔         |
| **最高值**   | 30000ms     | 30000ms     | 最高生成間隔（不變） |
| **初始值**   | 1000ms      | 2000ms      | 應用啟動時預設值     |
| **顯示格式** | 0.0-30.0 秒 | 2.0-30.0 秒 | 使用者可見範圍       |

---

## 🎯 限制範圍

```
原始範圍: 1.0秒 ~ 30.0秒
  ↓
新限制範圍: 2.0秒 ~ 30.0秒
  ↓
防止過度: 使用者無法設定低於 2 秒
  ↓
車流控制: 最小車輛生成速度受限
```

---

## 💡 設計用意

### 為什麼設定 2 秒最低值?

1. **防止過度生成**
   - 1 秒間隔 = 每秒 1 台車
   - 多方向 × 多車道 = 可能過載
   - 2 秒間隔 = 更合理的基線

2. **保護系統性能**
   - 過多車輛 → CPU 飆升
   - 碰撞檢測增加 → FPS 下降
   - 2 秒限制 → 保證穩定性

3. **防止誤操作**
   - 使用者無意中拉到最低
   - 系統突然被車流淹沒
   - 2 秒最低 → 安全邊界

4. **提供合理基線**
   - Peak Hour 情景 = 多秒間隔
   - Off-Peak 情景 = 長秒間隔
   - 2 秒最低 = 足夠的靈活性

---

## 🔍 驗證內容

| 檢查項目      | 結果                     |
| ------------- | ------------------------ |
| ✅ 拉桿最低值 | min="2000" (2 秒)        |
| ✅ 拉桿最高值 | max="30000" (30 秒)      |
| ✅ 初始化值   | manualInterval.ref(2000) |
| ✅ 初始顯示   | currentInterval.ref(2.0) |
| ✅ 編譯狀態   | 零錯誤                   |

---

## 📈 使用者影響

### UI 表現

```
舊版本:
[← 1.0秒 ────────────────────────→ 30.0秒]

新版本:
[← 2.0秒 ────────────────────────→ 30.0秒]
  ↑
  最低不再是 1 秒
```

### 拉桿行為

```
舊版本:
- 拉到最左 = 1.0 秒
- 快速生成大量車輛 ⚠️

新版本:
- 拉到最左 = 2.0 秒
- 合理的車流生成速度 ✅
```

---

## 🚀 後續效果

### 立即效果

- ✅ 使用者無法設定 < 2 秒
- ✅ 應用啟動預設 2 秒
- ✅ 保護系統穩定性

### 長期效果

- ✅ 防止高峰期過度車流
- ✅ 穩定系統性能
- ✅ 改善使用者體驗

---

## 📝 測試建議

### 快速驗證 (1分鐘)

```
1. 啟動應用: quasar dev
2. 觀察初始拉桿位置: 應為 2.0s
3. 嘗試拖拉拉桿:
   ✅ 最左應停在 2.0s (無法再往左)
   ✅ 最右應停在 30.0s
   ✅ 顯示值應正確 (2.0s ~ 30.0s)
```

### 功能驗證 (3分鐘)

```
1. 設定拉桿為 2.0s
2. 選擇 Peak Hour
3. 開始模擬，觀察:
   ✅ 車輛生成速度: 2 秒一台 ✓
   ✅ 系統性能: 穩定 ✓
   ✅ CPU 占用: 正常 ✓
```

### 邊界測試 (2分鐘)

```
1. 嘗試強制設定 < 2s: 應失敗 ✓
2. 設定 2.0s: 正常 ✓
3. 設定 30.0s: 正常 ✓
4. 動態調整 (2s → 30s): 流暢 ✓
```

---

## 📂 相關文檔

- **MainLayout.vue** - 主布局文件 (拉桿元件)
- **vehicleConfig.js** - 車輛配置 (生成參數)
- **trafficScenarioConfig.js** - 情景配置

---

## ✅ 修改清單

- [x] 拉桿最低值: 1000ms → 2000ms
- [x] 初始化值: 1000ms → 2000ms
- [x] 初始顯示: 1.0s → 2.0s
- [x] 編譯驗證: ✅ 零錯誤
- [x] 文檔記錄: ✅ 完成

---

## 🎓 技術說明

### HTML Range 屬性

```vue
<!-- min/max 屬性限制拉桿範圍 -->
<input
  type="range"
  min="2000"    <!-- 最低值（毫秒） -->
  max="30000"   <!-- 最高值（毫秒） -->
  step="100"    <!-- 每步增加 100ms -->
  v-model="manualInterval"
/>

<!-- v-model 綁定到 ref 變數 -->
<!-- 修改拉桿 → 自動更新 manualInterval 變數 -->
<!-- 變數更新 → 觸發 @input 事件 → 更新 UI -->
```

### Vue Ref 初始化

```javascript
// 參考值初始化
const manualInterval = ref(2000) // 預設 2000ms
const currentInterval = ref(2.0) // 顯示用 2.0 秒

// 渲染時這些值會被用到:
// 1. 拉桿位置 → manualInterval (2000)
// 2. 顯示文字 → (manualInterval / 1000).toFixed(1) = "2.0"
// 3. 後端邏輯 → currentInterval (2.0 秒)
```

---

## 🎯 成功標準

**修改成功** ≡

```
✅ 拉桿最低值為 2000ms
AND
✅ 初始值為 2000ms
AND
✅ 使用者無法調低於 2.0 秒
AND
✅ 編譯無誤
```

**當前狀態**: 🟢 **全部達成** ✅

---

**修改完成時間**: 2025-11-05
**編譯狀態**: ✅ 零錯誤
**驗證狀態**: ✅ 通過

**現在可以啟動應用測試了！** 🚀
