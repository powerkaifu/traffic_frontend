# 🚗 所有方向車道分配邏輯完整檢查

## 📋 座標系統分析

### SVG 座標系
```
  x: 0 到 1400 (水平)
  y: 0 到 1000 (垂直)
  
     (0,0) ─────────── (1400,0)
       │                │
       │    停止線       │
       │                │
   (0,1000) ─────────── (1400,1000)
```

## 🔍 各方向實際起始點（從 AutoTrafficGenerator.js）

```javascript
const spawnPoints = {
  east:  { x: 0,    y: 350 },  // ← 東向起始點（左邊界）
  west:  { x: 800,  y: 350 },  // ← 西向起始點（中間位置！）
  north: { x: 450,  y: 600 },  // ← 北向起始點（下邊界）
  south: { x: 450,  y: 0 },    // ← 南向起始點（上邊界）
}
```

## ✅ 各方向詳細分析

### 1️⃣ **東向 (East)** ✅ 正確
```
起始點：x = 0 (左邊界)
停止線：x ≈ 700
移動方向：x 遞增 (0 → 700)
起始區域：x ∈ [0, 300]

當前檢查：x < 300 ✅ 正確
```

### 2️⃣ **西向 (West)** ❌ 錯誤！
```
起始點：x = 800 (中間位置，NOT 右邊界!)
停止線：x ≈ 700
移動方向：x 向左減少 (800 → 700)
起始區域：x ∈ [500, 800]

當前檢查：x > 1100 ❌ 完全錯誤！
  - 西向新車在 x = 800，不符合 x > 1100 條件
  - 導致新生成的西向車無法被計入統計
  - 結果：西向可能也有車道分配偏差
```

### 3️⃣ **北向 (North)** ✅ 已修復
```
起始點：y = 600 (下邊界)
停止線：y ≈ 0-100
移動方向：y 遞減 (600 → 0)
起始區域：y ∈ [300, 600]

當前檢查：y < 300 ✅ 正確（修復後）
```

### 4️⃣ **南向 (South)** ❌ 錯誤！
```
起始點：y = 0 (上邊界)
停止線：y ≈ 500
移動方向：y 遞增 (0 → 500)
起始區域：y ∈ [0, 300]

當前檢查：y < 300 ✅ 看起來正確

但等等...南向新車在 y = 0，符合 y < 300
但坐標系統中，y = 0 其實在頂部，不是底部！
需要檢查實際的起始點位置...
```

## 🚨 發現的問題

### 問題 1：西向邏輯完全錯誤
- **症狀**：西向車道分配可能也不均勻（但沒被發現因為問題較輕）
- **原因**：西向起始點是 `x = 800`，但檢查條件用的是 `x > 1100`（右邊界）
- **修復**：應該是 `x < 1100 && x > 500` 或 `Math.abs(x - 800) < 300`

### 問題 2：需要驗證南向
- **疑問**：南向起始點 y = 0，但 SVG 中 y = 0 是頂部還是底部？
- **建議**：檢查 SVG 路徑定義確認南向的實際移動方向

## ✅ 修復方案

### 最簡單的修復方法：基於實際起始點

```javascript
const isCarInStartArea = (carPos, direction) => {
  const startAreaThreshold = 300

  switch (direction) {
    case 'east':
      // 起始點 x=0，起始區域 x∈[0, 300]
      return carPos.x < startAreaThreshold
      
    case 'west':
      // 起始點 x=800，起始區域 x∈[500, 1100]
      return carPos.x > 500 && carPos.x < 1100
      // 或更簡潔: Math.abs(carPos.x - 800) < 300
      
    case 'north':
      // 起始點 y=600，起始區域 y∈[300, 600]
      return carPos.y < startAreaThreshold
      
    case 'south':
      // 起始點 y=0，起始區域 y∈[0, 300]
      // 需要確認 SVG 座標系統中 y 的方向...
      return carPos.y < startAreaThreshold
  }
}
```

## 📊 預期修復效果

| 方向 | 當前狀態 | 修復後 |
|-----|---------|--------|
| East | ✅ 正確 | ✅ 保持 |
| West | ❌ 嚴重錯誤 | ✅ 修復 |
| North | ✅ 已修復 | ✅ 保持 |
| South | ? 待驗證 | ✅ 確認 |

