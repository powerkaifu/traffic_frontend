# 🚀 占有率計算快速參考

## 一眼對比

### 舊機制 ❌

```
maxCapacity = 60 (硬編碼)
baseOccupancy = 15% (固定)
結果：尖峰也是 15%，凌晨也是 15% → 失去時段區分
```

### 新機制 ✅

```
占有率 = 基礎% + (當前車輛 ÷ API最大) × (目標最大 - 目標最小) + 隨機波動
結果：尖峰 45-65%，離峰 20-40%，凌晨 8-18% → 完美時段感知
```

---

## 三時段速查表

### 📍 尖峰時段 (07:00-09:00, 17:00-19:00)

| 當前車輛 | API 限制 | 占有率 | 說明       |
| -------- | -------- | ------ | ---------- |
| 0 輛     | 30 輛    | 45%    | 基礎占有率 |
| 10 輛    | 30 輛    | 51.7%  | 低流量     |
| 15 輛    | 30 輛    | 55%    | 中流量     |
| 20 輛    | 30 輛    | 58.3%  | 高流量     |
| 30 輛    | 30 輛    | 65%    | 滿容量     |

**特性**：基數高 (45%)，範圍寬 (45-65%)

---

### 📍 離峰時段 (09:00-17:00, 19:00-23:00)

| 當前車輛 | API 限制 | 占有率 | 說明       |
| -------- | -------- | ------ | ---------- |
| 0 輛     | 20 輛    | 20%    | 基礎占有率 |
| 5 輛     | 20 輛    | 25%    | 低流量     |
| 10 輛    | 20 輛    | 30%    | 中流量     |
| 15 輛    | 20 輛    | 35%    | 高流量     |
| 20 輛    | 20 輛    | 40%    | 滿容量     |

**特性**：基數中等 (20%)，範圍標準 (20-40%)

---

### 📍 凌晨時段 (23:00-07:00)

| 當前車輛 | API 限制 | 占有率 | 說明       |
| -------- | -------- | ------ | ---------- |
| 0 輛     | 8 輛     | 8%     | 基礎占有率 |
| 2 輛     | 8 輛     | 10.5%  | 低流量     |
| 4 輛     | 8 輛     | 13%    | 中流量     |
| 6 輛     | 8 輛     | 15.5%  | 高流量     |
| 8 輛     | 8 輛     | 18%    | 滿容量     |

**特性**：基數低 (8%)，範圍窄 (8-18%)

---

## 🎲 隨機波動

### 第 1、2 次 API 呼叫時

```
隨機範圍 = (0.5 - Math.random()) × randomRange

尖峰 (randomRange=10)：  -5% 到 +5%
離峰 (randomRange=8)：   -4% 到 +4%
凌晨 (randomRange=5)：   -2.5% 到 +2.5%
```

### 示例

```
離峰 30%，隨機波動 ±4%
→ 可能是 26% 到 34% 之間
→ 模擬真實流量波動
```

---

## 🔧 配置詳解

### occupancyConfig 對象結構

```javascript
{
  peak_hours: {
    targetRange: [45, 65],      // 占有率目標範圍
    baseOccupancy: 45,          // 基礎占有率（0輛車時）
    randomRange: 10,            // 隨機波動幅度
    backendVehicles: 30,        // API 最多傳送的車輛數
  },
  // ... 其他時段
}
```

| 字段              | 含義                 | 示例值   |
| ----------------- | -------------------- | -------- |
| `targetRange`     | 占有率應在此範圍內   | [45, 65] |
| `baseOccupancy`   | 0 輛車時的起始占有率 | 45       |
| `randomRange`     | 隨機波動的最大範圍   | 10 (±5%) |
| `backendVehicles` | 作為 100% 的車輛數   | 30       |

---

## 📊 計算示例

### 場景：離峰時段，當前 10 輛車

```
步驟 1: 讀取配置
└─ targetRange: [20, 40]
└─ baseOccupancy: 20
└─ randomRange: 8
└─ backendVehicles: 20

步驟 2: 計算車輛比例
└─ vehicleRatio = min(10 / 20, 1.0) = 0.5

步驟 3: 計算基礎占有率
└─ vehicleBasedOccupancy = 20 + (40-20) × 0.5 = 30%

步驟 4: 加入隨機波動（假設 API 呼叫 #1）
└─ randomNoise = (0.5 - Math.random()) × 8
└─ 假設 randomNoise = 2
└─ finalOccupancy = 30 + 2 = 32%

步驟 5: 限制範圍 [0, 100]
└─ finalOccupancy = 32% (已在範圍內)

結果：32%
```

---

## ⚠️ 邊界情況

### 情況 1：0 輛車

```
各時段結果：
尖峰：  45%
離峰：  20%
凌晨：  8%

原因：基礎占有率（baseOccupancy）
```

### 情況 2：超過 API 最大車輛

```
例如：尖峰時段 40 輛車（API 最大 30 輛）

vehicleRatio = min(40/30, 1.0) = 1.0 (被限制)
占有率 = 45 + (65-45) × 1.0 = 65% (被限制在最大)
```

### 情況 3：占有率超過 100%

```
（理論上不應該發生，但有保護）
finalOccupancy = max(min(finalOccupancy, 100), 0)
→ 結果被限制在 100%
```

### 情況 4：無效時段

```
timePeriod 為 null 或未知
→ 使用默認值：'off_peak'
→ 占有率範圍：20-40%
```

---

## 🔍 驗證方法

### 在瀏覽器控制台測試

```javascript
// 獲取交通控制器實例
const controller = window.trafficLightController

// 測試不同場景
console.log('=== 尖峰時段 ===')
console.log(controller.calculateOccupancy('east')) // 應該 45-65%

console.log('=== 離峰時段 ===')
console.log(controller.calculateOccupancy('east')) // 應該 20-40%

console.log('=== 凌晨時段 ===')
console.log(controller.calculateOccupancy('east')) // 應該 8-18%
```

### 添加調試日誌

```javascript
// 在 calculateOccupancy 方法中添加
console.log({
  timePeriod,
  totalVehicles,
  vehicleRatio,
  vehicleBasedOccupancy,
  finalOccupancy: finalOccupancy.toFixed(1),
})
```

---

## 💡 設計思想

### 為什麼是這些數字？

```
尖峰時段 45-65%：
├─ 反映早晚尖峰的高流量
├─ 45% = 即使沒有車也有基礎擁堵
└─ 65% = 30 輛車滿負荷時

離峰時段 20-40%：
├─ 反映正常白天流量
├─ 20% = 輕載基礎
└─ 40% = 20 輛車滿負荷時

凌晨時段 8-18%：
├─ 反映深夜稀疏交通
├─ 8% = 極少車輛時
└─ 18% = 8 輛車滿負荷時
```

### 為什麼用 backendVehicles？

```
舊機制：
maxCapacity = 60 (憑空出現？)
→ 無法追溯

新機制：
backendVehicles = 30 / 20 / 8 (來自 VOLUME_LIMITS_CONFIG)
→ 直接對應 API 層實際限制
→ 科學、可追溯、易維護
```

---

## 🔄 工作流程圖

```
API 傳送數據 (30/20/8 輛)
    ↓
計算時段 (peak/off/night)
    ↓
讀取時段配置
    ↓
計算車輛比例 (當前/最大)
    ↓
計算基礎占有率 (線性映射)
    ↓
加入隨機波動 (API 初期)
    ↓
限制範圍 [0, 100%]
    ↓
返回占有率 (保留 1 位小數)
    ↓
前端顯示占有率
```

---

## 📝 改進前後對比表

| 方面           | 改進前        | 改進後                  |
| -------------- | ------------- | ----------------------- |
| **時段感知**   | ❌ 無         | ✅ 有 (3 種配置)        |
| **基礎占有率** | 15% (固定)    | 45% / 20% / 8% (時段)   |
| **最大容量**   | 60 (硬編碼)   | 30 / 20 / 8 (API)       |
| **占有率範圍** | 15-100%       | 45-65% / 20-40% / 8-18% |
| **隨機波動**   | 10-24% (固定) | ±10% / ±8% / ±5% (時段) |
| **準確度**     | ⭐⭐          | ⭐⭐⭐⭐⭐              |
| **可維護性**   | ⭐⭐          | ⭐⭐⭐⭐⭐              |

---

## 🎯 實際應用場景

### 場景 1：早上尖峰 7:30

```
API 返回：motor=18, small=8, large=2 → 28 輛
時段：peak_hours
占有率計算：
  vehicleRatio = 28/30 = 0.933
  occupancy = 45 + (65-45) × 0.933 = 63.7%

前端顯示：📊 63.7%
```

### 場景 2：中午離峰 12:00

```
API 返回：motor=8, small=5, large=1 → 14 輛
時段：off_peak
占有率計算：
  vehicleRatio = 14/20 = 0.7
  occupancy = 20 + (40-20) × 0.7 = 34%

前端顯示：📊 34%
```

### 場景 3：深夜 2:00

```
API 返回：motor=3, small=2, large=0 → 5 輛
時段：late_night
占有率計算：
  vehicleRatio = 5/8 = 0.625
  occupancy = 8 + (18-8) × 0.625 = 14.25%

前端顯示：📊 14.3%
```

---

## ✅ 快速檢驗清單

- [ ] 占有率永遠在 [0, 100] 範圍內？
- [ ] 尖峰時段占有率在 45-65%？
- [ ] 離峰時段占有率在 20-40%？
- [ ] 凌晨時段占有率在 8-18%？
- [ ] 車輛數越多，占有率越高？
- [ ] 時段改變時，占有率有適當變化？
- [ ] 第 1、2 次 API 呼叫有隨機波動？

---

## 📞 聯繫與支持

如有問題，請參考完整文檔：

- 📄 `OCCUPANCY_RATE_CALCULATION.md` - 詳細技術文檔
- 💻 `TrafficLightController.js` - 實現源代碼
- ⚙️ `vehicleConfig.js` - 配置參考

---

**最後更新**：commit `c74ffc2`
**狀態**：✅ 生產就緒
