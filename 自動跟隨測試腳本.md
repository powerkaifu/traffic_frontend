# 🧪 自動跟隨功能測試腳本

## 快速驗證（複製到 Console）

### 腳本 1：即時監控自動跟隨

```javascript
// 完整監控自動跟隨狀態
const monitorAutoFollow = setInterval(() => {
  const all = window.liveVehicles || []
  const autoFollowing = all.filter(v => v.currentState === 'autoFollowing')
  const stopped = all.filter(v => {
    const speed = v.movementTimeline?.timeScale()
    return speed === 0 && v.currentState === 'stopped'
  })
  
  console.clear()
  console.log('%c=== 自動跟隨監控 ===', 'color: cyan; font-size: 16px; font-weight: bold')
  console.log(`車輛總數: ${all.length}`)
  console.log(`%c🚗 自動跟隨: ${autoFollowing.length} 輛`, 'color: green')
  console.log(`⏸️  完全停止: ${stopped.length} 輛`)
  
  if (autoFollowing.length > 0) {
    console.log('\n%c自動跟隨中的車輛:', 'color: green; font-weight: bold')
    autoFollowing.forEach(v => {
      const speed = v.movementTimeline?.timeScale()
      const pos = v.getCurrentPosition()
      console.log(`  ${v.id}:`, {
        '速度': (speed*100).toFixed(0) + '%',
        '位置': `(${pos.x.toFixed(0)}, ${pos.y.toFixed(0)})`,
        '狀態': v.currentState
      })
    })
  }
}, 1000)

console.log('%c監控已啟動！', 'color: green; font-size: 14px')
console.log('停止監控: clearInterval(monitorAutoFollow)')
```

### 腳本 2：檢查碰撞和跟隨

```javascript
// 檢查車輛碰撞和跟隨狀態
function checkCollisionAndFollow() {
  const all = window.liveVehicles || []
  
  console.log('%c=== 碰撞與跟隨狀態 ===', 'color: blue; font-size: 16px')
  
  all.forEach(v => {
    const speed = v.movementTimeline?.timeScale()
    const state = v.currentState
    
    if (state === 'autoFollowing' || speed > 0 && speed < 0.3) {
      console.log(`%c車輛 ${v.id}`, 'color: orange; font-weight: bold')
      console.log('  狀態:', state)
      console.log('  速度:', (speed*100).toFixed(0) + '%')
      
      // 檢查前方車輛
      const collision = v.collisionController?.checkSimpleCollision(all)
      if (collision) {
        console.log('  碰撞檢測:', {
          距離: collision.distance?.toFixed(1) + 'px',
          自動跟隨: collision.autoFollowing,
          目標速度: (collision.targetSpeed*100).toFixed(0) + '%'
        })
      }
    }
  })
}

checkCollisionAndFollow()
```

### 腳本 3：追蹤特定車輛

```javascript
// 追蹤特定車輛的跟隨行為
function trackVehicle(vehicleId) {
  const vehicle = window.liveVehicles?.find(v => v.id === vehicleId)
  
  if (!vehicle) {
    console.log('找不到車輛:', vehicleId)
    return
  }
  
  const tracker = setInterval(() => {
    const speed = vehicle.movementTimeline?.timeScale()
    const state = vehicle.currentState
    const pos = vehicle.getCurrentPosition()
    
    console.log(`[${vehicleId}]`, {
      狀態: state,
      速度: (speed*100).toFixed(0) + '%',
      位置: `(${pos.x.toFixed(0)}, ${pos.y.toFixed(0)})`
    })
    
    // 檢查碰撞
    const collision = vehicle.collisionController?.checkSimpleCollision(window.liveVehicles)
    if (collision) {
      console.log('  碰撞:', {
        距離: collision.distance?.toFixed(1),
        自動跟隨: collision.autoFollowing,
        原因: collision.reason
      })
    }
  }, 500)
  
  console.log(`開始追蹤 ${vehicleId}`)
  console.log('停止追蹤: clearInterval(tracker)')
  
  return tracker
}

// 使用範例
// const tracker = trackVehicle('vehicle-123')
```

### 腳本 4：統計跟隨效果

```javascript
// 統計自動跟隨效果
function analyzeAutoFollow() {
  const all = window.liveVehicles || []
  const stats = {
    total: all.length,
    autoFollowing: 0,
    stopped: 0,
    moving: 0,
    speeds: {
      'very_slow (0-10%)': 0,
      'slow (10-20%)': 0,
      'medium (20-50%)': 0,
      'fast (>50%)': 0
    }
  }
  
  all.forEach(v => {
    const speed = v.movementTimeline?.timeScale() || 0
    const state = v.currentState
    
    if (state === 'autoFollowing') stats.autoFollowing++
    if (speed === 0) stats.stopped++
    if (speed > 0) stats.moving++
    
    if (speed > 0 && speed <= 0.1) stats.speeds['very_slow (0-10%)']++
    else if (speed > 0.1 && speed <= 0.2) stats.speeds['slow (10-20%)']++
    else if (speed > 0.2 && speed <= 0.5) stats.speeds['medium (20-50%)']++
    else if (speed > 0.5) stats.speeds['fast (>50%)']++
  })
  
  console.log('%c=== 自動跟隨統計 ===', 'color: purple; font-size: 16px')
  console.table(stats)
  console.log('\n速度分布:')
  console.table(stats.speeds)
}

// 每5秒執行一次
setInterval(analyzeAutoFollow, 5000)
```

## 測試步驟

### 1. 基本測試

1. **啟動監控**：執行腳本 1
2. **生成車輛**：調高車輛生成頻率
3. **觀察追撞**：等待後車快速接近前車
4. **檢查跟隨**：
   - ✅ 看到「自動跟隨: X 輛」
   - ✅ 速度在 5%-25% 之間
   - ✅ 車輛緩慢前進

### 2. 詳細測試

1. **執行腳本 2**：檢查碰撞和跟隨狀態
2. **觀察輸出**：
   ```
   車輛 vehicle-123
     狀態: autoFollowing
     速度: 12%
     碰撞檢測: { 距離: '18.5px', 自動跟隨: true, 目標速度: '12%' }
   ```
3. **確認距離**：車輛應該在 8-50px 範圍內跟隨

### 3. 追蹤測試

1. **找到追撞車輛**：
   ```javascript
   const vehicles = window.liveVehicles
   console.log('所有車輛:', vehicles.map(v => v.id))
   ```

2. **追蹤特定車輛**：
   ```javascript
   const tracker = trackVehicle('vehicle-123')
   ```

3. **觀察變化**：
   - 速度從 0% → 12% → 18% → 25%
   - 狀態從 stopped → autoFollowing
   - 距離從 10px → 15px → 20px → 25px

## 成功標準

### ✅ 應該看到

1. **監控輸出**：
   ```
   === 自動跟隨監控 ===
   車輛總數: 12
   🚗 自動跟隨: 3 輛
   ⏸️  完全停止: 5 輛
   
   自動跟隨中的車輛:
     vehicle-456: { 速度: '12%', 位置: '(520, 300)', 狀態: 'autoFollowing' }
   ```

2. **車輛行為**：
   - 碰撞後不是完全停止（speed ≠ 0）
   - 緩慢移動（speed = 5%-25%）
   - 狀態為 'autoFollowing'
   - 逐漸靠近前車

3. **最終結果**：
   - 車輛停在距前車約 25px
   - 間距合理，不會重疊
   - 不影響紅燈排隊

### ❌ 不應該看到

1. **完全停止不動**：
   - speed = 0 且距離 > 8px
   - 狀態為 'stopped' 但應該跟隨

2. **距離太近**：
   - 車輛重疊或碰撞
   - 距離 < 8px 仍在移動

3. **速度異常**：
   - 速度 > 30%（太快）
   - 速度 = 0 但距離很遠

## 參數調整測試

### 測試不同速度

```javascript
// 臨時修改配置（測試用）
const config = FOLLOWING_CONFIG.AUTO_FOLLOW_AFTER_COLLISION
config.FOLLOW_SPEEDS.CLOSE = 0.20 // 從 0.12 改為 0.20

// 觀察效果
// 重新生成車輛測試
```

### 測試不同距離

```javascript
// 臨時修改目標距離
const config = FOLLOWING_CONFIG.AUTO_FOLLOW_AFTER_COLLISION
config.TARGET_FOLLOW_DISTANCE = 30 // 從 25 改為 30

// 觀察車輛最終停止距離
```

## 調試技巧

### 加入 Console.log

在 CollisionController.js 第 753 行：

```javascript
if (followSpeed > 0) {
  console.log('%c🚗 自動跟隨啟動', 'color: lime; font-weight: bold', {
    車輛: this.vehicle.id,
    距離: distance.toFixed(1) + 'px',
    速度: (followSpeed*100).toFixed(0) + '%',
    閾值: '8-15-25-35-50px'
  })
  return { ... }
}
```

在 Vehicle.js 第 1072 行：

```javascript
if (shouldStop && shouldStop.autoFollowing) {
  console.log('%c✅ 接收到自動跟隨信號', 'color: green', {
    車輛: this.id,
    目標速度: (shouldStop.targetSpeed*100).toFixed(0) + '%',
    原因: shouldStop.reason
  })
}
```

## 常見問題

### 問題 1：沒有自動跟隨

**檢查**：
```javascript
const config = FOLLOWING_CONFIG.AUTO_FOLLOW_AFTER_COLLISION
console.log('功能啟用:', config.ENABLED)
console.log('最小距離:', config.MIN_FOLLOW_DISTANCE)
```

**解決**：確保 `ENABLED: true`

### 問題 2：速度太慢

**調整**：
```javascript
// vehicleConfig.js
FOLLOW_SPEEDS: {
  CLOSE: 0.18, // 從 0.12 提高
  NORMAL: 0.25, // 從 0.18 提高
}
```

### 問題 3：跟隨太早觸發

**調整**：
```javascript
// vehicleConfig.js
MIN_FOLLOW_DISTANCE: 12, // 從 8 提高
```

---

**複製腳本 1 到 Console，立即開始監控自動跟隨功能！** 🚀
