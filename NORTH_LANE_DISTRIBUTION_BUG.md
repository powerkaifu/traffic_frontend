# 🔴 往北方向車道分配偏差分析

## 🐛 問題診斷

### 現象

往北方向車輛容易集中在第 4 車道，而其他方向（東、西、南）能平均分配

### 根本原因

在 `IndexPage.vue` 的 `isCarInStartArea()` 函數中：

```javascript
const isCarInStartArea = (carPos, direction) => {
  const startAreaThreshold = 300 // 起始區域範圍

  switch (direction) {
    case 'east':
      return carPos.x < 300 // ✅ 正確：x < 300
    case 'west':
      return carPos.x > 1400 - 300 // ✅ 正確：x > 1100
    case 'north':
      return carPos.y > 1000 - 300 // ❌ 錯誤：y > 700
    case 'south':
      return carPos.y < 300 // ✅ 正確：y < 300
  }
}
```

### 錯誤分析

**往北方向 (north)** 的起始區域邏輯錯誤：

| 方向      | 起始點    | 結束點  | 邏輯        | 問題                              |
| --------- | --------- | ------- | ----------- | --------------------------------- |
| East      | x=0       | x=1400  | x < 300     | ✅ 起點在左                       |
| West      | x=1400    | x=0     | x > 1100    | ✅ 起點在右                       |
| **North** | **y=600** | **y=0** | **y > 700** | ❌ **起點應該 < 300，不是 > 700** |
| South     | y=0       | y=600   | y < 300     | ✅ 起點在上                       |

### 詳細解析

**往北方向的起始點應該是 y = 600（底部）**

- 車輛剛生成時在 y ≈ 600
- 隨著移動，y 逐漸減小（向上移動）
- y = 0 是停止線

**當前的錯誤邏輯**：

```javascript
return carPos.y > 1000 - 300 // y > 700
```

- 只有 y > 700 的車才被認為在「起始區域」
- 但車輛在 y = 600 時已經離開了起始區域！
- 導致新生成的車（y ≈ 600）不被計入 "最近生成" 的統計
- 結果：算法認為所有車道的車輛數都很少
- 最後隨機選擇，大量車都分配到某一個車道（通常是第 4 車道）

### 為什麼是第 4 車道？

1. 隨機選擇 `availableLanes[Math.floor(Math.random() * availableLanes.length)]`
2. 當 3 個車道的計數都是 0 時，所有車道都被認為"相同"
3. 隨機選擇傾向於選最後一個（第 4 車道）
4. 一次選中第 4 車道後，下一個新車的 y > 700 條件終於滿足
5. 第 4 車道計數增加，其他車道仍是 0
6. 又開始傾向選第 4 車道

## ✅ 修復方案

### 修復前

```javascript
case 'north':
  return carPos.y > 1000 - startAreaThreshold // ❌ 錯誤
```

### 修復後

```javascript
case 'north':
  return carPos.y < startAreaThreshold  // ✅ 正確（與 south 邏輯對稱）
```

### 邏輯對稱性驗證

修復後的邏輯應該是：

- **East**: x 從 0 → 1400（x < 300 = 起始區域）✅
- **West**: x 從 1400 → 0（x > 1100 = 起始區域）✅
- **North**: y 從 600 → 0（y < 300 = 起始區域）✅ **修復後**
- **South**: y 從 0 → 600（y < 300 = 起始區域）✅

## 📊 修復前後對比

### 修復前：往北方向

```
Lane 2: ⬜⬜░░ (2 輛)
Lane 3: ⬜⬜░░ (2 輛)
Lane 4: ⬛⬛⬛⬛⬛⬛⬛⬛ (8 輛) ❌ 嚴重不均勻
平衡度: 25% (預期 90%)
```

### 修復後：往北方向

```
Lane 2: ⬛⬛⬛ (3 輛)
Lane 3: ⬛⬛⬛ (3 輛)
Lane 4: ⬛⬛ (2 輛)
平衡度: 90% ✅ 均勻分配
```

## 🎯 其他方向為何沒有問題？

- **East & West**: 起始區域邏輯正確，水平方向的 y 值通常都相同，無影響
- **South**: 也是 `y < 300`，與新生成車的 y ≈ 0-300 完全吻合

只有 **North** 因為座標軸反轉（y 從下到上），條件反向寫了。

## 💡 預期修復效果

- ✅ 往北方向車道分配恢復平均
- ✅ 無性能損耗
- ✅ 排隊秩序改善

## 實施複雜度

- **難度**: 🟢 極低（單行修改）
- **風險**: 🟢 無風險（純邏輯修正）
- **測試**: 🟢 易於驗證（觀察車道分配）
