# 真實交通變化系統使用指南

## ✅ 已完成的工作

**所有 24 個時段已啟用真實變化系統**，讓每個時段的參數不再固定，更貼近真實交通的波動特性。

## 📊 變化效果總覽

### 不同時段的變化幅度

| 時段                         | 變化類型     | 參數變化幅度 | 說明                   |
| ---------------------------- | ------------ | ------------ | ---------------------- |
| **早/晚尖峰** (07-09, 17-19) | `peak`       | ±3-8%        | 上下班流量穩定，變化小 |
| **白天離峰** (09-17, 19-21)  | `off_peak`   | ±6-12%       | 一般時段，中等變化     |
| **凌晨/深夜** (00-06, 21-24) | `late_night` | ±10-20%      | 流量稀少不規則，變化大 |

### 實際變化示例

**早尖峰 08:00** - 每次獲取配置都會稍微不同：

- 生成間隔: 1500ms → 可能是 **1380-1620ms**
- 機車比例: 45% → 可能是 **43-47%**
- 速度: 35-50 km/h → 可能是 **32-53 km/h**

## 🎯 正常現象確認

### ✅ 這些是正常的

- 面板上的「生成間隔」數字會跳動
- 車型比例會微調（總和仍是 100%）
- 速度範圍會小幅變化
- 每次時段切換時參數會更新

### ⚠️ 這些需要注意

- 數值變化過大（超出 ±20%）
- 車型比例總和不是 100%
- 出現負數或異常值

---

## 📝 如何自行修改參數（重要！）

### 方法 1: 調整整體變化幅度

**位置**: `src/classes/config/trafficVariationHelper.js`

找到 `applySmartVariation` 函數，修改變化幅度：

```javascript
switch (timePeriod) {
  case 'peak':
    // 尖峰時段：變化較小，流量相對穩定
    options = {
      intervalVariation: 8, // 👈 改這裡：±8% → 可改為 5-15
      vehicleTypeVariation: 3, // 👈 改這裡：±3% → 可改為 2-8
      speedVariation: 3, // 👈 改這裡：±3 km/h → 可改為 2-10
      multiplierVariation: 0.05, // 👈 改這裡：±0.05 → 可改為 0.02-0.15
    }
    break

  case 'late_night':
    // 凌晨時段：變化較大
    options = {
      intervalVariation: 20, // 👈 改這裡：±20% → 可改為 15-30
      vehicleTypeVariation: 10, // 👈 改這裡：±10% → 可改為 5-15
      speedVariation: 8, // 👈 改這裡：±8 km/h → 可改為 5-12
      multiplierVariation: 0.15, // 👈 改這裡：±0.15 → 可改為 0.1-0.2
    }
    break

  case 'off_peak':
  default:
    // 離峰時段：中等變化
    options = {
      intervalVariation: 12, // 👈 改這裡：±12% → 可改為 8-18
      vehicleTypeVariation: 6, // 👈 改這裡：±6% → 可改為 4-10
      speedVariation: 5, // 👈 改這裡：±5 km/h → 可改為 3-8
      multiplierVariation: 0.1, // 👈 改這裡：±0.1 → 可改為 0.05-0.15
    }
    break
}
```

**修改建議**:

- 太穩定 → 增加百分比
- 太混亂 → 減少百分比
- 修改後重新載入頁面即生效

### 方法 2: 調整週期波動幅度

**位置**: `src/classes/config/trafficVariationHelper.js`

找到 `applyCyclicVariation` 函數：

```javascript
// 5分鐘週期：小幅波動
const shortCycle = Math.sin((elapsedMinutes / 5) * Math.PI * 2) * 0.05 // 👈 0.05 = ±5%

// 15分鐘週期：中幅波動
const mediumCycle = Math.sin((elapsedMinutes / 15) * Math.PI * 2) * 0.1 // 👈 0.1 = ±10%

// 30分鐘週期：大幅波動
const longCycle = Math.sin((elapsedMinutes / 30) * Math.PI * 2) * 0.08 // 👈 0.08 = ±8%
```

**修改建議**:

- 不想要週期波動 → 將數值改為 `0`
- 想要更明顯波動 → 增加數值（如 `0.15`）
- 想要更平滑波動 → 減少數值（如 `0.03`）

### 方法 3: 為特定時段自訂變化

**位置**: `src/classes/config/trafficScenarioConfig.js`

找到想修改的時段（例如早尖峰），調整變化參數：

```javascript
// 早尖峰時段 (07:00-09:00)
else if (currentHour >= 7 && currentHour < 9) {
  const baseConfig = {
    name: `早尖峰 ${currentHour}:00`,
    interval: { min: 1000, max: 5000, normal: 1500 },
    // ...其他參數
  }

  return applyFullRealisticVariation(baseConfig, {
    timePeriod: 'peak',              // 👈 改這裡：使用不同的預設變化
    elapsedMinutes: currentTime.getMinutes() + currentHour * 60,
    enableCyclicVariation: true,     // 👈 改這裡：false = 關閉週期波動
  })
}
```

**可選的 `timePeriod` 值**:

- `'peak'` - 小變化 (±3-8%)
- `'off_peak'` - 中等變化 (±6-12%)
- `'late_night'` - 大變化 (±10-20%)

**關閉週期波動**:

```javascript
enableCyclicVariation: false,  // 關閉 5/15/30 分鐘週期
```

### 方法 4: 完全關閉某個時段的變化

在 `trafficScenarioConfig.js` 中，將該時段改回直接 `return baseConfig`：

```javascript
// 例如：讓早尖峰完全固定，不再變化
else if (currentHour >= 7 && currentHour < 9) {
  return {  // 👈 直接 return，不經過 applyFullRealisticVariation
    name: `早尖峰 ${currentHour}:00`,
    interval: { min: 1000, max: 5000, normal: 1500 },
    // ...其他參數
  }
}
```

---

## 🧪 測試變化效果

### 快速測試腳本

在控制台執行，查看特定時段的變化：

```javascript
// 測試早尖峰 08:00 的變化
console.log('=== 早尖峰變化測試 ===')
for (let i = 0; i < 5; i++) {
  const time = new Date()
  time.setHours(8, i * 10, 0, 0)
  const config = getScenarioByTime(time)
  console.log(`第 ${i + 1} 次:`, config.interval.normal, 'ms')
}
```

### 完整對比測試

```javascript
// 測試不同時段的變化幅度
;['凌晨 3:00', '早尖峰 8:00', '午間 12:00', '晚尖峰 18:00', '夜間 22:00'].forEach((label, idx) => {
  const hours = [3, 8, 12, 18, 22][idx]
  const time = new Date()
  time.setHours(hours, 0, 0, 0)

  const results = []
  for (let j = 0; j < 5; j++) {
    time.setMinutes(j * 10)
    const config = getScenarioByTime(time)
    results.push(config.interval.normal)
  }

  const min = Math.min(...results)
  const max = Math.max(...results)
  const variation = (((max - min) / ((max + min) / 2)) * 100).toFixed(1)

  console.log(`${label}: 變化幅度 ${variation}%`)
})
```

---

## � 進階自訂

### 創建自己的變化模式

在 `trafficVariationHelper.js` 中新增：

```javascript
// 在 applySmartVariation 函數中添加新的 case
case 'custom_heavy':  // 👈 自訂的重度變化
  options = {
    intervalVariation: 25,
    vehicleTypeVariation: 15,
    speedVariation: 10,
    multiplierVariation: 0.2,
  }
  break
```

然後在 `trafficScenarioConfig.js` 中使用：

```javascript
return applyFullRealisticVariation(baseConfig, {
  timePeriod: 'custom_heavy', // 👈 使用自訂模式
  elapsedMinutes: currentTime.getMinutes() + currentHour * 60,
  enableCyclicVariation: true,
})
```

---

## � 常見調整場景

### 場景 1: 尖峰時段太混亂

**問題**: 上下班時間流量變化太大  
**解決**: 在 `trafficVariationHelper.js` 中減少 `peak` 的變化幅度

```javascript
case 'peak':
  options = {
    intervalVariation: 5,   // 原本 8 → 改為 5
    vehicleTypeVariation: 2, // 原本 3 → 改為 2
    // ...
  }
```

### 場景 2: 凌晨時段想更穩定

**問題**: 凌晨流量太不規則  
**解決**: 在 `trafficVariationHelper.js` 中減少 `late_night` 的變化幅度

```javascript
case 'late_night':
  options = {
    intervalVariation: 10,  // 原本 20 → 改為 10
    vehicleTypeVariation: 5, // 原本 10 → 改為 5
    // ...
  }
```

### 場景 3: 想要更明顯的週期波動

**問題**: 流量起伏不夠明顯  
**解決**: 在 `trafficVariationHelper.js` 的 `applyCyclicVariation` 中增加幅度

```javascript
const shortCycle = Math.sin((elapsedMinutes / 5) * Math.PI * 2) * 0.1 // 0.05 → 0.1
const mediumCycle = Math.sin((elapsedMinutes / 15) * Math.PI * 2) * 0.15 // 0.1 → 0.15
const longCycle = Math.sin((elapsedMinutes / 30) * Math.PI * 2) * 0.12 // 0.08 → 0.12
```

---

## 🔍 檢查清單

修改參數後，請確認：

- [ ] 變化範圍合理（不會有負數或過大的值）
- [ ] 車型比例總和仍是 100%
- [ ] 不同時段的變化幅度符合預期
- [ ] 視覺效果更真實自然
- [ ] 性能沒有明顯下降

---

**相關檔案**:

- `src/classes/config/trafficVariationHelper.js` - 變化系統核心（調整變化幅度）
- `src/classes/config/trafficScenarioConfig.js` - 時段配置（套用變化系統）

**最後更新**: 2025-11-20
